# 敲脑壳 MindPop - 项目介绍

> 专为复杂知识设计的硬核记忆工具
> 强制主动提取 × 科学间隔复习 × 结构化记忆

---

## 项目概述

| 项目信息 | 详情 |
|---------|------|
| **项目名称** | 敲脑壳 MindPop |
| **项目定位** | 面向「一题多答案」场景的主动提取训练工具 |
| **开发模式** | 独立设计开发（AI 辅助编码） |
| **开发周期** | 2026.02–2026.03 主力开发（2026.01 为方向探索期） |
| **迭代节奏** | 7.5 周迭代 15 个版本（v1.1.0 → v1.10.2） |
| **在线访问** | https://mindpop.top |
| **技术栈** | Spring Boot 2.7 + MySQL 8.0 + JWT + 原生 JavaScript；本地 MCP Server 使用 TypeScript |
| **仓库** | https://github.com/niiwei/mindpop |

---

## 为什么做它

我在电商业务中需要快速熟悉一整套分类体系（打点体系），只能靠背。作为 Anki 的长期用户，我发现它在**一题多答案**的场景下不成立：

- **闪卡只有正反面，没有中间状态**，无法渐进式披露答案；而一个打点下面往往有多个子打点。
- **闪卡没有答案匹配机制**——你不知道自己背得对不对，于是会退回「直接看一眼答案，然后以为自己会了」。

这正是我想避免的。所以我把它做成「测验式多答案 + 输入即匹配」：敲对关键词，答案才跳出来。

---

## 核心功能

### 1. 强制主动输出 ——「单框连击」交互

- 无需手动提交，**输入即匹配**
- 不敲对不显示，强迫完成一次真正的提取
- 无序答题，任意顺序命中答案要点

### 2. 结构化答案 —— 必答关键词 + 补充语境

- 答案由「必答关键词」与「补充语境」组成，语境不参与匹配、但保留完整语句的阅读体验
- 一条答案可拆成多个可独立判定的要点，答对一部分先点亮一部分
- 支持免输入正文与注释，注释不参与匹配

### 3. 科学间隔复习 —— SM-2 算法

- 基于 Anki 同款 SM-2 间隔重复算法计算下次复习时间
- 4 级评级（重来 / 困难 / 良好 / 简单），easeFactor 在 1300–5000 区间动态调整
- 5 种状态流转：新测验 → 学习中 → 待复习 → 今日复习 → 重学中

### 4. 用户系统与测验管理

- JWT 无状态认证 + BCrypt 密码加密
- 账户数据严格隔离（所有查询按 userId 过滤）
- 测验分组（多对多）、学习统计、JSON 导入导出
- 12 个官方测验包，新用户注册即获

---

## 关键设计与取舍

- **答案不该是整句，只该是必答关键词。** 依据来自我自己的记忆行为：背的时候只需要记住 llms.txt、skill.md、token 数这类关键词，不需要记住「发布」「编写」这类连接词。这条后来直接定义了训练任务的数据格式。
- **未作答时答案卡片保持空白。** 不加提示、不加计数——任何额外说明都是泄题，会稀释提取难度。
- **标记语法要三方友好。** 同时满足便于后端处理、便于人工编辑、便于 Agent 编辑。
- **删除操作需要两步确认。** 永久删除前生成短期确认，确认失效或资源已变化时不能继续。

---

## 技术实现

### 后端

```
Controller → Service → Repository → Entity
     ↓            ↓              ↓
  REST API    业务逻辑      Spring Data JPA
```

- Spring Boot 2.7 + Spring Data JPA / Hibernate
- MySQL 8.0，支持级联删除与多表关联
- JWT（JJWT）+ Spring Security（BCrypt）
- **80+ 方法级 REST 接口，分布于 16 个 Controller**
- 使用 `JOIN FETCH` 消除测验分组与答案查询中的 N+1 问题

### 前端

- 原生 JavaScript（ES6+），无框架依赖
- 响应式布局与即时匹配交互
- Fetch API 异步请求，LocalStorage 本地状态

### 部署与工程

- Docker 容器化 + 云服务器部署，Nginx 反代
- `main` 作为唯一发布源；GitHub Actions CI（含 Playwright 浏览器测试）
- 密钥只通过环境变量或未跟踪的 `.env.local` 注入，不入库

---

## Agent 化能力（2026.09）

让 AI 能够安全地读写个人题库，是本项目第二阶段的核心目标。

- **版本化 Agent API**（`/api/agent/v1`）：与网页登录的 JWT 通道隔离，PAT 只能访问该接口
- **PAT 个人访问令牌**：可为本地 Agent 创建长期凭证，可命名、可单独撤销，完整令牌仅在创建时展示一次
- **本地 MCP Server**：TypeScript 实现的 stdio MCP Server（Node 20+），Agent 通过严格 schema 调用题库能力；第一版仅 TYPING 题型可写，其余只读
- **幂等导入**：带稳定 requestId 的批量写入意图，同一请求可安全重试
- **乐观锁版本校验与导入事务回滚**

---

## 开发历程

| 阶段 | 时间 | 里程碑 |
|------|------|--------|
| v1.0 – v1.2 | 2026.01 | 基础打字测验、用户系统、填空题 |
| v1.3 – v1.5 | 2026.01 末 – 02 初 | 测验管理增强、UI 优化、Docker 部署 |
| v1.6 | 2026.02.08 | **间隔重复复习系统（核心功能）** |
| v1.8 | 2026.02.22 | 极简风 UI 全面重构 |
| v1.9 | 2026.02.23 | 测验分组体系重构 |
| v1.10 | 2026.03.06 | 官方测验包 —— 进站即学 |
| v1.10.2 | 2026.03.22 | 引导页与视频体验优化 |
| **Agent 化** | **2026.09** | 答案要点结构、Agent API、PAT、MCP Server、CI |

> 开发工具：2026 年 1–3 月使用 Windsurf 辅助编码；2026 年 9 月的 Agent 化阶段改用 Codex，工作流为「需求澄清 → spec → 拆票 → 实现 → 子 Agent 隔离自测 → code review」。

---

## 在线体验

**访问地址：** https://mindpop.top

**试用账户：** 用户名 `test8` / 密码 `test8`

| 页面 | 功能 |
|------|------|
| `/home.html` | 首页复习看板（今日待复习统计） |
| `/quizzes.html` | 测验列表（分组筛选） |
| `/review.html` | 复习模式（SM-2 算法驱动） |
| `/stats.html` | 学习统计（今日 / 本周 / 本月） |

---

## 本地启动

```bash
# macOS / Linux
./start.sh --check   # 首次环境检查
./start.sh

# Windows
start.bat
```

访问 http://localhost:8080（若配置了其他端口，以实际配置为准）。
完整步骤见 `docs/internal/LOCAL_DEVELOPMENT.md`。

---

## 相关项目

- **后训练：题库答案拆分小模型** —— 为本项目的题目自动生成能力训练专用小模型。本项目 v2 的答案结构（必答关键词 + 补充语境）即该模型的训练任务定义（`required / context` 划分）。

---

**仓库：** https://github.com/niiwei/mindpop
**作者主页：** https://niiwei.github.io
