# 复制为 .env.local；由 start.sh 作为 shell 配置加载，仅填写可信内容。
# 显式设置优先于自动发现；路径必须指向包含 bin/java 的 JDK 11 根目录。
# JAVA_HOME=/absolute/path/to/jdk-11
# MYSQL_HOST=your_mysql_host
# MYSQL_USER=your_mysql_user
# MYSQL_PASSWORD='your_mysql_password'
# JWT_SECRET='your_jwt_secret'
# SERVER_PORT=8080
# 当前云端库经 ./start.sh --tunnel 访问时，使用下列三项替代 MYSQL_*：
# SPRING_DATASOURCE_URL='jdbc:mysql://127.0.0.1:13306/your_database?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8'
# SPRING_DATASOURCE_USERNAME='your_mysql_user'
# SPRING_DATASOURCE_PASSWORD='your_mysql_password'
